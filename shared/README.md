# Shared extension code

Place implementation here only when it is used by adapters for two or more harnesses. Keep harness lifecycle hooks, manifests, configuration, and installation logic beneath `harnesses/<name>/`.

