# Shared extension code

Place implementation here only when it is used by two or more workspace
packages. Keep lifecycle hooks, native manifests, configuration, and
installation logic inside the package that publishes them.
