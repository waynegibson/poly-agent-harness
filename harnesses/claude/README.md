# Claude Code adapter

Portable skills are installed through `~/.claude/skills` or a project's `.claude/skills` directory. Keep Claude-only plugins, commands, agents, hooks, and configuration beneath this directory.

