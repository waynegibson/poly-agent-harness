# Portable skills

Each child directory is a canonical Agent Skill containing a required `SKILL.md` file and optional `scripts/`, `references/`, `assets/`, and harness metadata.

Do not put harness-specific extensions in this directory. Run `npm run validate` after adding or renaming a skill.

